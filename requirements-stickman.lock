# Direct runtime dependency lock for the isolated service image. Test-only
# dependencies are installed by CI and are intentionally excluded from production.
# Demo/WebRTC dependencies (gradio, aiortc, av) are intentionally excluded: the
# Stickman service loads the inference model and processor directly and does not
# expose the upstream demo/UI/WebRTC surfaces.
# The accepted image digest becomes the deployable artifact after target-machine smoke.
accelerate==1.6.0
transformers==4.51.3
datasets==3.5.0
peft==0.15.2
llvmlite==0.44.0
numba==0.61.2
diffusers==0.33.1
tqdm==4.67.1
numpy==2.1.2
scipy==1.15.2
librosa==0.11.0
ml-collections==1.1.0
absl-py==2.2.2
fastapi==0.116.1
uvicorn[standard]==0.35.0
pydantic==2.11.7
